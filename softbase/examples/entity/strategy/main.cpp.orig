//#include <Porto>
#include <iostream>
#include "unit.h"

int main (int argc, char *argv[])
{
//   porto::init(argc, argv);

   auto storage = createStorage("mongodb")
      .addOption("database", "porto")
      .addOption("collection", "mystorage");

#if 0
   auto transactor = porto::createTransactor(storage);
#endif

   Unit unit;
   unit.abbreviation         = "Gy/s";
   unit.code                 = "0780";
   unit.conversionMultiplier = 1.0e0;
   unit.conversionOffset     = 0.0;
   unit.quantityKind         = "quantity:AbsorbedDoseRate";
   unit.symbol               = "Gy/s";
   unit.unit                 = "GrayPerSecond";
#if 0   
   transactor.entityList() << unit;
   transactor.store();
#endif

   std::cout << unit.code << std::endl;
   return EXIT_SUCCESS;
}
